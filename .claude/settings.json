{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/.forrest/session_start.py\" --agent claude-code"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${FORREST_WORKSPACE:-}\" = \"1\" ]; then if [ -z \"${FORREST_WORK_HOOK:-}\" ]; then echo \"Forrest managed tool hook is unavailable\" >&2; exit 2; fi; python3 \"$FORREST_WORK_HOOK\"; fi",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${FORREST_WORKSPACE:-}\" = \"1\" ]; then if [ -z \"${FORREST_WORK_HOOK:-}\" ]; then echo \"Forrest managed tool hook is unavailable\" >&2; exit 2; fi; python3 \"$FORREST_WORK_HOOK\"; fi",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${FORREST_WORKSPACE:-}\" = \"1\" ]; then if [ -z \"${FORREST_WORK_HOOK:-}\" ]; then echo \"Forrest managed tool hook is unavailable\" >&2; exit 2; fi; python3 \"$FORREST_WORK_HOOK\"; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
